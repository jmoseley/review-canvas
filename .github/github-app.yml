scripts:
  - name: Setup
    command: npm install
    triggers:
      - session.create

  - name: Run
    command: npm run dev

server_ready_pattern: '(?i)Local:\s+(https?://\S+)'

automation:
  auto_issue_session: true
  remote_control: false
